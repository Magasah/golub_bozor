# 🎯 Руководство по динамическим формам с HTMX

## 📋 Обзор

Динамическая форма создания объявлений использует **HTMX** для загрузки специфичных полей в зависимости от выбранной категории животного. Это улучшает UX и обеспечивает релевантный набор полей для каждого типа животного.

---

## 🏗️ Архитектура

### Компоненты системы

1. **Модель Animal** (`core/models.py`)
   - Базовые поля (title, price, description, city, photos, video)
   - **Категорийно-специфичные поля**:
     - Голуби: `pigeon_breed`, `flight_duration`, `game_style`, `gender_pigeon`
     - Скот (коровы/овцы/лошади/козы): `weight`, `age`, `livestock_breed`, `gender_livestock`
     - Питомцы (собаки/кошки): `pet_breed`, `has_passport`
     - Общие поля: `breed`, `gender`, `age`

2. **Динамическая форма** (`core/forms_dynamic.py`)
   - `DynamicAnimalForm` с HTMX атрибутами на поле `category`
   - Метод `clean()` для валидации обязательных полей по категориям
   - Автоматическое добавление CSS классов

3. **View-функции** (`core/views.py`)
   - `add_animal()` - главная view для создания объявления (использует `DynamicAnimalForm`)
   - `load_category_fields()` - HTMX endpoint для загрузки специфичных полей

4. **Шаблоны**
   - `templates/core/animal_form.html` - главный шаблон формы (5 шагов)
   - `templates/core/partials/form_fields.html` - HTMX partial с условными блоками

---

## 🔄 Как работает HTMX

### Схема взаимодействия

```
1. Пользователь выбирает категорию (например, "Голуби")
   ↓
2. HTMX отправляет GET запрос на /load-category-fields/?category=pigeon
   ↓
3. Django view load_category_fields() рендерит partials/form_fields.html
   ↓
4. HTMX заменяет содержимое <div id="specific-fields"> на полученный HTML
   ↓
5. Появляются поля: pigeon_breed, flight_duration, game_style, gender_pigeon
```

### HTMX атрибуты в форме

```html
<!-- В forms_dynamic.py -->
'category': forms.Select(attrs={
    'hx-get': '/load-category-fields/',  # URL для загрузки
    'hx-target': '#specific-fields',      # Куда вставлять HTML
    'hx-trigger': 'change',               # Когда загружать (при изменении)
    'hx-indicator': '#loading-indicator'  # Показать индикатор загрузки
})
```

---

## 🎨 Дизайн и UX

### Цветовая схема Black/Gold Luxury
- **Фон**: `#121212` (темный)
- **Карточки**: `#1E1E1E` с border `#2A2A2A`
- **Золотой акцент**: `#D4AF37` (заголовки, кнопки, focus-ring)
- **Текст**: `text-white`, `text-gray-300`

### Адаптивность
```html
<!-- Responsive grid -->
<div class="grid grid-cols-1 md:grid-cols-2 gap-6">
    <!-- Поля -->
</div>
```
- **Mobile**: 1 колонка
- **Desktop**: 2 колонки

### Анимации
```css
@keyframes fade-in {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
}
```

---

## 📝 Категории и их поля

### 🕊️ Голуби (pigeon)
**Обязательные поля**:
- `pigeon_breed` - Порода голубя (текст)
- `gender_pigeon` - Пол (выбор: male/female/pair)

**Дополнительные поля**:
- `flight_duration` - Длительность полета (текст)
- `game_style` - Стиль игры/бой (текст)

**Пример**:
```
Порода: Бойный Узбекский
Пол: Самец
Длительность полета: 3-4 часа
Стиль игры: Столбовой бой
```

---

### 🐄 Скот (cow, sheep, horse, goat)
**Обязательные поля**:
- `livestock_breed` - Порода (текст)
- `weight` - Вес в кг (decimal, например: 350.50)
- `age` - Возраст (текст, например: "2 года")
- `gender_livestock` - Пол (выбор: male/female)

**Пример**:
```
Порода: Гиссарская овца
Вес: 85 кг
Возраст: 18 месяцев
Пол: Самка
```

---

### 🐕 Питомцы (dog, cat)
**Обязательные поля**:
- `pet_breed` - Порода (текст)

**Дополнительные поля**:
- `gender` - Пол (выбор: male/female/unknown)
- `age` - Возраст (текст)
- `has_passport` - Есть вет. паспорт (checkbox, boolean)

**Пример**:
```
Порода: Британская короткошерстная
Пол: Самка
Возраст: 1 год
✅ Есть ветеринарный паспорт/прививки
```

---

### 🦜 Птицы (parrot, bird_other)
**Поля**:
- `pet_breed` - Вид/Порода (текст)
- `gender` - Пол (выбор: male/female/pair/unknown)
- `age` - Возраст (текст)

**Пример**:
```
Вид: Жако
Пол: Не определен
Возраст: 6 месяцев
```

---

### 🦁 Другие категории
**Поля**:
- `breed` - Порода/Вид (текст)
- `gender` - Пол (выбор: male/female/unknown)
- `age` - Возраст (текст)

---

## 🛠️ Валидация формы

### Backend валидация (forms_dynamic.py)

```python
def clean(self):
    category = self.cleaned_data.get('category')
    
    if category == 'pigeon':
        if not self.cleaned_data.get('pigeon_breed'):
            raise ValidationError('Укажите породу голубя')
        if not self.cleaned_data.get('gender_pigeon'):
            raise ValidationError('Укажите пол голубя')
    
    elif category in ['cow', 'sheep', 'horse', 'goat']:
        if not self.cleaned_data.get('weight'):
            raise ValidationError('Укажите вес животного')
        # ... остальные проверки
```

### Frontend индикация (HTMX)
```html
<!-- Индикатор загрузки -->
<div id="loading-indicator" class="htmx-indicator">
    <div class="animate-spin">⏳</div> Загрузка полей...
</div>
```

---

## 🔧 Настройка и расширение

### Добавление новой категории

**Шаг 1**: Добавьте выбор в `CATEGORY_CHOICES` (models.py)
```python
CATEGORY_CHOICES = [
    ('pigeon', 'Голуби'),
    ('new_category', 'Новая категория'),  # ← Добавить
]
```

**Шаг 2**: Добавьте специфичные поля в модель
```python
class Animal(models.Model):
    # ... существующие поля
    new_field = models.CharField(max_length=200, blank=True, null=True)
```

**Шаг 3**: Обновите `DynamicAnimalForm.Meta.fields`
```python
fields = [
    'category', 'title', 'price', 'city', 'description',
    'pigeon_breed', ..., 'new_field'  # ← Добавить
]
```

**Шаг 4**: Добавьте валидацию в `clean()` метод
```python
elif category == 'new_category':
    if not self.cleaned_data.get('new_field'):
        raise ValidationError('Укажите new_field')
```

**Шаг 5**: Добавьте HTML блок в `partials/form_fields.html`
```django
{% elif category == 'new_category' %}
<!-- Новые поля -->
<div class="space-y-6 animate-fade-in">
    <h2>Характеристики новой категории</h2>
    <input type="text" name="new_field" />
</div>
{% endif %}
```

**Шаг 6**: Создайте и примените миграцию
```bash
python manage.py makemigrations
python manage.py migrate
```

---

## 🧪 Тестирование

### Проверка HTMX функциональности

1. **Откройте форму**: http://127.0.0.1:8000/animal/add/
2. **Выберите категорию**: Голуби
3. **Проверьте**:
   - Появился индикатор загрузки (⏳ Загрузка полей...)
   - Блок "Характеристики голубя" с полями: pigeon_breed, gender_pigeon, flight_duration, game_style
   - Анимация fade-in

4. **Смените категорию**: Скот → Корова
5. **Проверьте**:
   - Блок изменился на "Характеристики животного" с полями: livestock_breed, gender_livestock, weight, age

### Проверка валидации

1. **Заполните форму**: Выберите "Голуби", НЕ заполните pigeon_breed
2. **Отправьте**: Должна появиться ошибка "Укажите породу голубя"
3. **Заполните все обязательные поля**: Отправьте → Success

---

## 📊 Performance

### Оптимизация HTMX запросов

- **Кэширование**: partials используют Django template caching
- **Минимальный payload**: Загружаются только нужные поля (не вся страница)
- **Debouncing**: `hx-trigger="change"` срабатывает только при фактическом изменении

### Метрики
- **Размер partial response**: ~2-4 KB (зависит от категории)
- **Время загрузки**: < 100ms (локальный сервер)
- **Network requests**: 1 дополнительный запрос при смене категории

---

## 🐛 Troubleshooting

### Проблема: Поля не загружаются при выборе категории

**Решение**:
1. Проверьте консоль браузера (F12 → Console) на HTMX ошибки
2. Убедитесь, что URL `/load-category-fields/` существует:
   ```bash
   python manage.py show_urls | grep load-category-fields
   ```
3. Проверьте, что `partials/form_fields.html` существует:
   ```
   templates/core/partials/form_fields.html
   ```

### Проблема: Ошибка "This field is required" для необязательных полей

**Решение**:
1. Убедитесь, что поля в модели имеют `blank=True, null=True`
2. В форме не добавляйте `required=True` для необязательных полей
3. Валидация должна быть только в `clean()` методе

### Проблема: Стили не применяются

**Решение**:
1. Проверьте, что TailwindCSS подключен в `base.html`:
   ```html
   <script src="https://cdn.tailwindcss.com"></script>
   ```
2. Обновите страницу с Ctrl+F5 (очистка кэша)

---

## 📚 Дополнительные ресурсы

- **HTMX документация**: https://htmx.org/docs/
- **TailwindCSS**: https://tailwindcss.com/docs
- **Django Forms**: https://docs.djangoproject.com/en/5.0/topics/forms/

---

## ✅ Чек-лист для разработчиков

- [ ] Новая категория добавлена в `CATEGORY_CHOICES`
- [ ] Специфичные поля добавлены в модель `Animal`
- [ ] Поля добавлены в `DynamicAnimalForm.Meta.fields`
- [ ] Валидация добавлена в `clean()` метод
- [ ] HTML блок добавлен в `partials/form_fields.html`
- [ ] Миграции созданы и применены (`makemigrations` + `migrate`)
- [ ] Форма протестирована для новой категории
- [ ] Валидация протестирована (обязательные поля)
- [ ] UI/UX проверено на мобильных устройствах

---

**Создано**: 11 января 2026  
**Версия**: 1.0  
**Автор**: GitHub Copilot (Claude Sonnet 4.5)
