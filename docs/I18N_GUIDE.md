# Руководство по мультиязычности (i18n) в ZooBozor

## 📝 Настройка завершена!

Проект ZooBozor теперь поддерживает мультиязычность на **Русском (RU)** и **Таджикском (TJ)**.

---

## 🔧 Что уже сделано

### 1. **Конфигурация settings.py**
```python
LANGUAGE_CODE = 'ru'

LANGUAGES = [
    ('ru', _('Русский')),
    ('tg', _('Тоҷикӣ')),
]

LOCALE_PATHS = [
    BASE_DIR / 'locale',
]

# Добавлен LocaleMiddleware в MIDDLEWARE
'django.middleware.locale.LocaleMiddleware',
```

### 2. **URL конфигурация (config/urls.py)**
```python
from django.conf.urls.i18n import i18n_patterns

urlpatterns += i18n_patterns(
    path('control_panel_secret_7828/', admin.site.urls),
    path('', include('core.urls')),
    prefix_default_language=False,
)
```

### 3. **Переключатель языков в base.html**
- Добавлен в навигационную панель (топ-правое меню)
- Дизайн: "🇷🇺 RU | 🇹🇯 TJ"
- Текущий язык отображается золотым (#D4AF37), другой - серый
- При клике язык меняется для всего сайта

---

## 📚 Как использовать переводы

### Способ 1: В Python файлах (models.py, views.py и т.д.)

```python
from django.utils.translation import gettext_lazy as _

# В моделях (verbose_name):
class Animal(models.Model):
    title = models.CharField(
        max_length=200,
        verbose_name=_('Название'),
        help_text=_('Название объявления')
    )
    
    category = models.CharField(
        max_length=50,
        verbose_name=_('Категория'),
        help_text=_('Вид животного')
    )

# В представлениях:
def my_view(request):
    message = _('Добро пожаловать!')
    return render(request, 'template.html', {'message': message})
```

### Способ 2: В HTML шаблонах

```django
{% load i18n %}

<!-- Простой перевод строки -->
<h1>{% trans "Добавить животное" %}</h1>

<!-- С параметрами -->
<p>{% blocktrans count counter=animals_count %}
    У вас есть {{ counter }} животное
{% plural %}
    У вас есть {{ counter }} животных
{% endblocktrans %}</p>

<!-- Использование переменных -->
<p>{% trans "Привет" %}, {{ user.username }}!</p>
```

### Способ 3: В forms.py

```python
from django.utils.translation import gettext_lazy as _

class AnimalForm(forms.ModelForm):
    class Meta:
        model = Animal
        fields = ['title', 'category', 'description']
        labels = {
            'title': _('Название'),
            'category': _('Категория'),
            'description': _('Описание'),
        }
```

---

## 🎯 Примеры переводимых элементов

### Уже переведено в base.html:
- "Добавить" → {% trans "Добавить" %}
- "Избранное" → {% trans "Избранное" %}
- "Мои объявления" → {% trans "Мои объявления" %}
- "Настройки" → {% trans "Настройки" %}
- "Выход" → {% trans "Выход" %}
- "Вход" → {% trans "Вход" %}
- "Регистрация" → {% trans "Регистрация" %}

### Нужно добавить переводы в другие шаблоны:
1. `templates/core/home.html`
2. `templates/core/animal_detail.html`
3. `templates/core/animal_form.html`
4. И другие пользовательские шаблоны

---

## 🌐 Генерирование файлов переводов

После того как вы пометили строки для перевода, запустите:

```bash
# Создать файлы переводов
python manage.py makemessages -l ru
python manage.py makemessages -l tg

# Это создаст файлы:
# locale/ru/LC_MESSAGES/django.po
# locale/tg/LC_MESSAGES/django.po
```

### Редактирование переводов (django.po):

```po
#: core/models.py:120
msgid "Название"
msgstr "Nomи"  # Таджикский перевод

#: core/models.py:125
msgid "Категория"
msgstr "Категория"  # Можно оставить так же
```

### Компиляция переводов:

```bash
python manage.py compilemessages
```

Это создаст `.mo` файлы, которые Django использует для работы.

---

## 🔑 Модель User Profile с кошельком

### Пример использования:

```python
from django.contrib.auth.models import User
from core.models import UserProfile, Transaction

# Получить профиль пользователя
user = User.objects.get(username='john_doe')
profile = user.profile

# Проверить баланс
print(f"Баланс: {profile.balance} TJS")

# Пополнить баланс
profile.add_balance(100, description="Пополнение через систему")

# Списать средства
try:
    profile.deduct_balance(50)
    print("Успешно списано 50 TJS")
except ValueError as e:
    print(f"Ошибка: {e}")

# Просмотреть историю транзакций
transactions = Transaction.objects.filter(user=user).order_by('-created_at')
for t in transactions:
    print(f"{t.get_transaction_type_display()}: {t.amount} TJS - {t.description}")
```

---

## 💾 Водяной знак на фотографиях

### Как это работает:

1. При загрузке фото животного автоматически добавляется водяной знак
2. Текст "ZooBozor" расположен в правом нижнем углу
3. Текст белый, полупрозрачный (50% opacity) с черной тенью
4. Читается на любом фоне

### Пример в моделе Animal:

```python
def save(self, *args, **kwargs):
    # ...
    if self.main_photo:
        self._add_watermark()  # Автоматическое добавление
    super().save(*args, **kwargs)
```

---

## 🔐 Admin панель

### Новые возможности:

1. **Управление кошельками** (Transaction Admin):
   - Просмотр всех транзакций
   - Фильтр по типу транзакции
   - Поиск по пользователю
   - История не может быть удалена или отредактирована

2. **Пополнение баланса пользователя**:
   - На странице UserProfile выберите пользователя
   - Нажмите экшен "💳 Пополнить баланс"
   - Введите сумму и описание
   - Баланс и история обновятся автоматически

---

## 📋 Чек-лист для полного внедрения

- [x] Настройки языков в settings.py
- [x] LocaleMiddleware добавлен
- [x] URL patterns для i18n
- [x] Переключатель языков в base.html
- [x] Модель Transaction создана
- [x] Balance добавлен в UserProfile
- [x] Methods deduct_balance и add_balance
- [x] Admin interface для управления кошельком
- [x] Водяной знак на фотографиях животных
- [ ] **TODO:** Создать locale/ru/LC_MESSAGES/django.po файлы
- [ ] **TODO:** Перевести все строки на таджикский
- [ ] **TODO:** Запустить compilemessages
- [ ] **TODO:** Добавить переводы в другие шаблоны

---

## 🚀 Следующие шаги

1. **Генерируем сообщения для перевода:**
   ```bash
   python manage.py makemessages -l ru
   python manage.py makemessages -l tg
   ```

2. **Редактируем файлы переводов:**
   - Откройте `locale/tg/LC_MESSAGES/django.po`
   - Заполните таджикские переводы

3. **Компилируем переводы:**
   ```bash
   python manage.py compilemessages
   ```

4. **Тестируем:**
   - Откройте сайт
   - Переключайте язык
   - Проверяйте, что текст меняется

---

## 💡 Советы

- Используйте `gettext_lazy` в моделях, а не обычный `gettext`
- Не переводите названия переменных и функций
- Переводите только пользовательский контент
- Для плюрализации используйте `blocktrans count`
- Регулярно запускайте `makemessages` когда добавляете новые строки

---

**Версия:** 1.0  
**Дата:** 26.01.2026  
**Проект:** ZooBozor - Premium Pigeon Marketplace
