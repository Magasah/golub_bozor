# 🎯 ИНСТРУКЦИЯ ПО УСТАНОВКЕ СИСТЕМЫ АУКЦИОНОВ v2.0

## 📋 ЧТО ВНУТРИ

Эта папка содержит все файлы для добавления системы аукционов на сайт GolubBozor.

### Файлы:
- `models_update.py` - Новые поля для Pigeon + модель Bid
- `forms_update.py` - Формы для аукциона (BidForm, обновлённый PigeonForm)
- `views_update.py` - Логика ставок (place_bid, обновлённый pigeon_detail)
- `admin_update.py` - Админ-панель для ставок
- `urls_update.py` - Новый маршрут для ставок
- `detail_template.html` - Обновлённый шаблон детальной страницы с UI аукциона
- `0005_auction_system.py` - Миграция базы данных
- `INSTALLATION.md` - Эта инструкция

---

## 🚀 УСТАНОВКА (ЛОКАЛЬНО)

### Шаг 1: Обновить models.py
1. Открой `core/models.py`
2. **В НАЧАЛО файла** добавь:
   ```python
   LISTING_TYPE_CHOICES = [
       ('fixed', 'Фиксированная цена'),
       ('auction', 'Аукцион'),
   ]
   ```

3. **В класс Pigeon** добавь новые поля (смотри `models_update.py` строки 15-77):
   - `listing_type`
   - `start_price`
   - `current_price`
   - `auction_end_date`
   - `is_sold`
   - `winner`
   - Методы: `is_auction_active()`, `get_highest_bid()`, `get_bid_count()`

4. **В КОНЕЦ файла** добавь новую модель `Bid` (смотри `models_update.py` строки 81-122)

---

### Шаг 2: Обновить forms.py
1. Открой `core/forms.py`
2. **В класс PigeonForm**:
   - Добавь в `fields`: `'listing_type', 'start_price', 'auction_end_date'`
   - Добавь в `widgets`:
     ```python
     'auction_end_date': forms.DateTimeInput(
         attrs={'type': 'datetime-local'},
         format='%Y-%m-%dT%H:%M'
     ),
     ```
   - Добавь метод `clean()` для валидации (смотри `forms_update.py` строки 7-32)

3. **В КОНЕЦ файла** добавь новую форму `BidForm` (смотри `forms_update.py` строки 36-76)

---

### Шаг 3: Обновить views.py
1. Открой `core/views.py`
2. **Добавь импорты** в начало:
   ```python
   from .models import Bid
   from .forms import BidForm
   ```

3. **В КОНЕЦ файла** добавь новую функцию `place_bid` (смотри `views_update.py` строки 11-49)

4. **Обнови функцию `pigeon_detail`**:
   - Добавь получение ставок: `bids = pigeon.bids.all()[:10]`
   - Добавь проверку завершения аукциона (смотри `views_update.py` строки 54-68)
   - Добавь создание формы ставки: `bid_form = BidForm(pigeon=pigeon)`
   - Добавь `bid_form` в context

---

### Шаг 4: Обновить admin.py
1. Открой `core/admin.py`
2. **Добавь импорт**:
   ```python
   from .models import Bid
   ```

3. **В КОНЕЦ файла** добавь `BidAdmin` (смотри `admin_update.py` строки 7-17)

4. **Обнови PigeonAdmin** - добавь новые поля в `list_display`, `list_filter`, `fieldsets` (смотри `admin_update.py` строки 21-47)

---

### Шаг 5: Обновить urls.py
1. Открой `core/urls.py`
2. **В список `urlpatterns`** добавь:
   ```python
   path('pigeon/<int:pigeon_id>/bid/', views.place_bid, name='place_bid'),
   ```

---

### Шаг 6: Обновить шаблон
1. **Замени** `templates/core/detail.html` на `detail_template.html` из этой папки
   - ИЛИ скопируй код вручную, если хочешь сохранить свой дизайн

---

### Шаг 7: Создать миграцию
```bash
python manage.py makemigrations
```

**ИЛИ** скопируй готовую миграцию:
1. Скопируй `0005_auction_system.py` в папку `core/migrations/`
2. Открой файл и измени `dependencies`:
   ```python
   dependencies = [
       ('core', '0004_pigeon_age'),  # Замени на твою последнюю миграцию!
   ]
   ```

---

### Шаг 8: Применить миграцию
```bash
python manage.py migrate
```

---

### Шаг 9: Проверить локально
```bash
python manage.py runserver
```

Перейди на любую страницу голубя и проверь:
- ✅ Отображается тип продажи (фиксированная/аукцион)
- ✅ Есть форма для ставки (если аукцион активен)
- ✅ История ставок отображается
- ✅ Обратный отсчёт времени работает

---

## 🌐 ДЕПЛОЙ НА PYTHONANYWHERE

### Шаг 1: Загрузить изменения на GitHub
```bash
git add .
git commit -m "Добавлена система аукционов v2.0"
git push origin main
```

---

### Шаг 2: Обновить код на сервере
1. Зайди на https://www.pythonanywhere.com
2. Открой **Bash console**
3. Выполни:
   ```bash
   cd ~/golub_bozor
   git pull origin main
   ```

---

### Шаг 3: Применить миграции
В той же Bash console:
```bash
source venv/bin/activate
python manage.py migrate
```

---

### Шаг 4: Собрать статику
```bash
python manage.py collectstatic --noinput
```

---

### Шаг 5: Перезагрузить сайт
1. Перейди на вкладку **Web**
2. Нажми зелёную кнопку **Reload magaj.pythonanywhere.com**

---

### Шаг 6: Проверить
Открой https://magaj.pythonanywhere.com и проверь работу аукциона!

---

## 🎨 КАК ИСПОЛЬЗОВАТЬ

### Для продавца:
1. Зайди в админ-панель: https://magaj.pythonanywhere.com/admin/
2. Добавь новый голубь
3. Выбери **Тип продажи**: "Аукцион"
4. Укажи **Начальную цену** (например, 100 TJS)
5. Укажи **Дату окончания аукциона**
6. Сохрани

### Для покупателя:
1. Открой страницу голубя с аукционом
2. Введи сумму ставки (минимум = текущая цена + 1 TJS)
3. Нажми "Сделать ставку"
4. Твоя ставка отобразится в истории

### Завершение аукциона:
- Когда время истекает, система автоматически определяет победителя
- Победитель = пользователь с самой высокой ставкой
- Голубь помечается как "Продан"

---

## 📊 НОВЫЕ ВОЗМОЖНОСТИ

✅ **Два типа продажи**: Фиксированная цена и Аукцион
✅ **Система ставок**: Пользователи делают ставки в реальном времени
✅ **Автоматическое определение победителя**: Когда аукцион завершается
✅ **История ставок**: Отображается топ-10 ставок
✅ **Обратный отсчёт**: До окончания аукциона в реальном времени
✅ **Защита**: Нельзя ставить на свой голубь, минимальная ставка, проверка времени
✅ **Админ-панель**: Полный контроль над ставками и аукционами

---

## ❗ ВАЖНО

1. **СДЕЛАЙ БЭКАП БД перед миграцией**:
   ```bash
   cp db.sqlite3 db.sqlite3.backup
   ```

2. **На сервере тоже сделай бэкап**:
   ```bash
   cp ~/golub_bozor/db.sqlite3 ~/golub_bozor/db.sqlite3.backup
   ```

3. **Проверь настройки времени** в `settings.py`:
   ```python
   USE_TZ = True
   TIME_ZONE = 'Asia/Dushanbe'
   ```

4. **Если что-то сломалось**, откати миграцию:
   ```bash
   python manage.py migrate core 0004_pigeon_age
   git reset --hard HEAD~1
   ```

---

## 🆘 ЕСЛИ ВОЗНИКЛИ ПРОБЛЕМЫ

### Ошибка миграции:
```bash
python manage.py migrate --fake-initial
```

### Статика не загружается:
```bash
python manage.py collectstatic --clear --noinput
```

### Изменения не отображаются:
- Ctrl+F5 (жесткая перезагрузка браузера)
- Проверь, что нажал Reload на PythonAnywhere

---

## 📧 КОНТАКТ

Если нужна помощь: @team_neoxe

---

**Удачи с запуском аукционов! 🎉**
