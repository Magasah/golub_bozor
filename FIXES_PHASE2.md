# ФИКСЫ ФАЗЫ 2 - Категория-специфичные формы

## 🎯 Проблема
Форма показывала ВСЕ поля для каждой категории (например, для транспорта показывались поля breed/age/gender что неправильно для таксиста)

## ✅ Решение

### 1. Удаление нефункциональных кнопок языков (RU/TJ)
- **Файл**: `templates/base.html`
- **Действие**: Удалены все кнопки переключения языков
  - Удалены из десктопной навигации (строки 591-624)
  - Удалены из мобильного меню (строки 950-988)
- **Причина**: Кнопки не работали и создавали путаницу

### 2. Упрощение form-fields.js
- **Файл**: `static/js/form-fields.js`
- **Изменения**:
  - Переписана функция `updateFieldsForCategory()` для управления видимостью контейнеров
  - Упрощены функции `showField()` и `hideField()`
  - Transport категория показывает ТОЛЬКО свои поля

### 3. Добавление недостающих полей в шаблон
- **Файл**: `templates/core/add_animal.html`
- **Добавлено**:
  - Transport fields контейнер с полями:
    - `transport_type` (тип транспорта)
    - `route_from` (маршрут откуда)
    - `route_to` (маршрут куда)
  - Все остальные условные поля (в скрытом виде):
    - `gender_livestock` (пол скота)
    - `weight` (вес)
    - `color_variety` (расцветка)
    - `flight_duration` (длительность полета)
    - `game_style` (стиль игры)
    - `health_status` (статус здоровья)

## 📊 Структура категорий

### Transport (Зоо-Такси)
- ✅ `transport_type`
- ✅ `route_from`
- ✅ `route_to`
- ❌ breed
- ❌ age
- ❌ gender
- ❌ price (видит только общие)

### Pigeon (Голуби)
- ✅ breed
- ✅ age
- ✅ gender
- ✅ color_variety
- ✅ flight_duration
- ✅ game_style

### Livestock (Скот: коровы, козы и т.д.)
- ✅ breed
- ✅ age
- ✅ gender_livestock
- ✅ weight
- ✅ health_status

### Pet (Питомцы: кошки, собаки и т.д.)
- ✅ breed
- ✅ age
- ✅ gender
- ✅ color_variety
- ✅ health_status

### Fish & Other (Рыбы и другие)
- ✅ breed (опционально)
- ✅ age (опционально)
- ✅ gender

## 🧪 Тестирование
1. Откроют форму добавления животного
2. Выбирают "Зоо-Такси / Ташвиқот" в категории
3. Проверяют что видны ТОЛЬКО:
   - Название
   - Тип транспорта
   - Маршрут от
   - Маршрут до
   - Цена
   - Описание
   - И общие поля (фото, контакты, видео)
4. Выбирают "Голуби / Кафтарҳо"
5. Проверяют что видны поля специфичные для голубей

## 🔄 Автоматизация
Form-fields.js автоматически:
- Слушает изменение категории через select[name="category"]
- Скрывает все условные контейнеры
- Показывает только нужные поля для выбранной категории
- Очищает значения скрытых полей перед отправкой

## 📝 JavaScript логика
```javascript
// Transport - ТОЛЬКО эти поля видны
transport: {
    visible: ['transport_type', 'route_from', 'route_to'],
    hidden: ['breed', 'age', 'gender', ...остальные]
}

// При выборе категории обновляются видимые контейнеры
// #transportFields - показывается для transport
// #animalDetailsFields - скрывается для transport
```

## ✨ Результат
- ✅ Таксист видит ТОЛЬКО транспортные поля (не путается с breed/age/gender)
- ✅ Заводчик голубей видит ТОЛЬКО свои поля (не видит weight/livestock полей)
- ✅ Профессиональная, ориентированная на категорию UX
- ✅ Нету нефункциональных UI элементов (удалены кнопки языков)
