# 🚀 ИНСТРУКЦИЯ ПО ПУБЛИКАЦИИ GOLUB BOZOR НА RENDER.COM

## 📝 ЧТО ТАКОЕ RENDER?
- **Бесплатный хостинг** для Python/Django проектов
- **Не требует**: паспорт, карту для бесплатного плана
- **Нужен только**: email и GitHub аккаунт
- **Домен**: yourapp.onrender.com (бесплатный)
- **⚠️ Минус**: сервер "засыпает" через 15 минут неактивности (первая загрузка ~30 секунд)

---

## 🎯 ПОШАГОВАЯ ИНСТРУКЦИЯ

### ШАГ 1: ПОДГОТОВКА ПРОЕКТА (5 минут)

Нужно создать несколько файлов для Render.

#### 1.1 Создай файл `build.sh` в корне проекта:

Это скрипт установки, который запустится на сервере.

**Открой терминал и выполни:**
```powershell
cd C:\Users\spart\Documents\Project\golub_bozor
```

**Создай файл `build.sh` с таким содержимым:**
```bash
#!/usr/bin/env bash
set -o errexit

pip install -r requirements.txt
python manage.py collectstatic --no-input
python manage.py migrate
```

#### 1.2 Обнови `requirements.txt`:

Добавим `gunicorn` - нужен для production сервера.

**Файл `requirements.txt` должен быть таким:**
```
Django==5.0.14
Pillow==12.0.0
django-axes==8.1.0
requests==2.32.5
gunicorn==21.2.0
whitenoise==6.6.0
psycopg2-binary==2.9.9
```

---

### ШАГ 2: НАСТРОЙКА SETTINGS.PY (3 минуты)

Нужно обновить настройки для работы на Render.

**В файле `config/settings.py` найди и измени:**

```python
# В начале файла добавь:
import dj_database_url

# После MIDDLEWARE добавь WhiteNoise для статики:
MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'whitenoise.middleware.WhiteNoiseMiddleware',  # Добавь эту строку
    'django.contrib.sessions.middleware.SessionMiddleware',
    # остальное без изменений...
]

# Настройки статики (замени существующие):
STATIC_URL = '/static/'
STATIC_ROOT = BASE_DIR / 'staticfiles'
STATICFILES_DIRS = [BASE_DIR / 'static']
STATICFILES_STORAGE = 'whitenoise.storage.CompressedManifestStaticFilesStorage'

# База данных - добавь поддержку PostgreSQL:
DATABASES = {
    'default': dj_database_url.config(
        default=f'sqlite:///{BASE_DIR / "db.sqlite3"}',
        conn_max_age=600
    )
}

# ALLOWED_HOSTS - обнови:
ALLOWED_HOSTS = ['localhost', '127.0.0.1', '.onrender.com']
```

---

### ШАГ 3: СОЗДАЙ GITHUB РЕПОЗИТОРИЙ (7 минут)

Render работает только через GitHub (это обязательно).

#### 3.1 На своем компьютере:

```powershell
cd C:\Users\spart\Documents\Project\golub_bozor

# Инициализируй Git
git init

# Добавь все файлы
git add .

# Сделай первый коммит
git commit -m "Initial commit for Render deployment"
```

#### 3.2 На GitHub:

1. Открой: https://github.com/new
2. Имя репозитория: `golub_bozor`
3. Можно сделать **Приватным** (Private) - это бесплатно
4. НЕ добавляй README, .gitignore, license
5. Нажми **"Create repository"**

#### 3.3 Загрузи код:

Скопируй команды с GitHub (после создания репозитория) или используй эти:

```powershell
git remote add origin https://github.com/твой-username/golub_bozor.git
git branch -M main
git push -u origin main
```

**Введи свой GitHub username и password/token когда попросит.**

---

### ШАГ 4: РЕГИСТРАЦИЯ НА RENDER (3 минуты)

1. Открой: https://render.com
2. Нажми **"Get Started for Free"**
3. Выбери **"Sign up with GitHub"**
4. Разреши Render доступ к GitHub
5. Подтверди email (проверь почту)

**Никаких карт, паспортов не требуется!**

---

### ШАГ 5: СОЗДАНИЕ WEB SERVICE (10 минут)

#### 5.1 Создание сервиса:

1. В Dashboard нажми **"New +"** → **"Web Service"**
2. Нажми **"Connect a repository"**
3. Найди репозиторий `golub_bozor` и нажми **"Connect"**

#### 5.2 Настройка сервиса:

Заполни форму:

| Поле | Значение |
|------|----------|
| **Name** | `golub-bozor` (будет домен: golub-bozor.onrender.com) |
| **Region** | `Frankfurt (EU Central)` или ближайший |
| **Branch** | `main` |
| **Root Directory** | оставь пустым |
| **Runtime** | `Python 3` |
| **Build Command** | `./build.sh` |
| **Start Command** | `gunicorn config.wsgi:application` |
| **Instance Type** | **Free** (выбери бесплатный план) |

#### 5.3 Переменные окружения (Environment Variables):

Прокрути вниз до секции **"Environment Variables"** и добавь:

Нажми **"Add Environment Variable"** для каждой:

| KEY | VALUE |
|-----|-------|
| `PYTHON_VERSION` | `3.11.7` |
| `DEBUG` | `False` |
| `SECRET_KEY` | `сгенерируй-случайную-строку-50-символов` |
| `DATABASE_URL` | оставь пустым (Render создаст автоматически) |

**Для SECRET_KEY используй:**
```
gjdk49fj2kf9dkf0d9kf0dk3f9dkf0d9kf0d9kf0d9kf0dk
```
(или любую случайную строку из букв и цифр)

#### 5.4 Запуск:

1. Прокрути вниз
2. Нажми **"Create Web Service"**
3. Render начнет деплой (займет 3-5 минут)

**Следи за логами** - увидишь процесс установки пакетов, миграций и т.д.

---

### ШАГ 6: СОЗДАНИЕ БАЗЫ ДАННЫХ PostgreSQL (5 минут)

Бесплатный план включает PostgreSQL базу данных.

#### 6.1 Создание БД:

1. В Dashboard нажми **"New +"** → **"PostgreSQL"**
2. Заполни:
   - **Name**: `golub-bozor-db`
   - **Database**: `golub_bozor`
   - **User**: автоматически
   - **Region**: тот же что у Web Service
   - **Instance Type**: **Free**
3. Нажми **"Create Database"**
4. Подожди 1-2 минуты

#### 6.2 Подключение БД к сервису:

1. Открой свой **Web Service** (`golub-bozor`)
2. Открой вкладку **"Environment"**
3. Найди переменную `DATABASE_URL`
4. Нажми **"Add from Database"**
5. Выбери свою базу `golub-bozor-db`
6. Нажми **"Save Changes"**

**Render автоматически перезапустит сервис!**

---

### ШАГ 7: СОЗДАНИЕ СУПЕР-ПОЛЬЗОВАТЕЛЯ (3 минуты)

Чтобы зайти в админку, нужно создать супер-пользователя.

#### 7.1 Через Shell:

1. Открой свой Web Service
2. Перейди в вкладку **"Shell"** (справа вверху)
3. Нажми **"Launch Shell"**
4. В консоли выполни:

```bash
python manage.py createsuperuser
```

5. Введи:
   - Username: `admin` (или любое)
   - Email: можно пропустить (Enter)
   - Password: придумай надежный
   - Подтверди password

#### 7.2 Альтернатива - через код:

Если Shell не работает, создай файл `create_admin.py`:

```python
import os
import django

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'config.settings')
django.setup()

from django.contrib.auth import get_user_model
User = get_user_model()

if not User.objects.filter(username='admin').exists():
    User.objects.create_superuser('admin', 'admin@example.com', 'твой-пароль')
    print('Superuser created!')
```

Добавь в `build.sh` перед последней строкой:
```bash
python create_admin.py
```

Закоммить и запушить:
```powershell
git add .
git commit -m "Add admin creation"
git push
```

Render автоматически передеплоит.

---

### ШАГ 8: ПРОВЕРКА РАБОТЫ (2 минуты)

1. Открой вкладку **"Logs"** - проверь что нет ошибок
2. Скопируй ссылку на свой сайт (вверху страницы)
   - Будет примерно: `https://golub-bozor.onrender.com`
3. Открой в браузере

**🎉 САЙТ ДОЛЖЕН РАБОТАТЬ!**

Проверь:
- Главную страницу
- Регистрацию/вход
- Добавление объявления
- Админку: `https://golub-bozor.onrender.com/admin/`

---

## ⚠️ ВАЖНО: "ЗАСЫПАНИЕ" СЕРВЕРА

Бесплатный план Render имеет ограничение:
- Сервер **засыпает** через **15 минут** без активности
- При следующем заходе **пробуждается ~30-50 секунд**
- Это нормально для бесплатного плана

**Решения:**
1. Принять как есть (для начала норм)
2. Использовать сервис ping (например, UptimeRobot - бесплатно пингует каждые 5 минут)
3. Перейти на платный план ($7/месяц - без засыпания)

---

## 🔧 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Проверь логи:

В Web Service → вкладка **"Logs"**

### Частые проблемки:

**1. Build Failed (ошибка при установке)**
- Проверь `build.sh` - правильные ли команды
- Проверь `requirements.txt` - все ли пакеты есть
- В логах будет видна конкретная ошибка

**2. Application failed to respond**
- Проверь `Start Command`: должно быть `gunicorn config.wsgi:application`
- Проверь логи - там будет ошибка запуска
- Убедись что `DATABASE_URL` настроен

**3. Статика не загружается (CSS/картинки)**
- Проверь что `whitenoise` установлен
- В `settings.py` должно быть `WhiteNoiseMiddleware`
- Перезапусти: **Manual Deploy** → **Clear build cache & deploy**

**4. Database Error**
- Убедись что PostgreSQL база создана
- Проверь что `DATABASE_URL` подключен к сервису
- В логах будет видна ошибка подключения

**5. "DisallowedHost" ошибка**
- В `settings.py` проверь: `ALLOWED_HOSTS = ['.onrender.com']`

---

## 🔄 КАК ОБНОВИТЬ САЙТ ПОСЛЕ ИЗМЕНЕНИЙ

Render автоматически деплоит при `git push`:

```powershell
cd C:\Users\spart\Documents\Project\golub_bozor

# Внеси изменения в код...

git add .
git commit -m "Описание изменений"
git push
```

Render увидит push и автоматически:
1. Запустит `build.sh`
2. Установит новые пакеты (если есть)
3. Применит миграции
4. Перезапустит сервис

**Следи за процессом во вкладке "Events"**

### Ручной деплой:

Если автодеплой не сработал:
1. Открой Web Service
2. Нажми **"Manual Deploy"**
3. Выбери **"Clear build cache & deploy"** (если были изменения в зависимостях)

---

## 📊 ДОСТУП К АДМИНКЕ

После запуска открой:
```
https://golub-bozor.onrender.com/admin/
```

Войди с данными супер-пользователя.

---

## 💡 ПОЛЕЗНЫЕ КОМАНДЫ (в Shell)

```bash
# Открыть Django shell
python manage.py shell

# Создать новую миграцию
python manage.py makemigrations

# Применить миграции
python manage.py migrate

# Собрать статику
python manage.py collectstatic --no-input

# Проверить проект на ошибки
python manage.py check

# Посмотреть логи базы данных
python manage.py dbshell
```

---

## 🎯 БЕСПЛАТНЫЙ ПЛАН - ОГРАНИЧЕНИЯ

✅ **Что можно:**
- Web Service + PostgreSQL база
- 512MB RAM
- Неограниченный bandwidth
- HTTPS сертификат (бесплатно)
- Автодеплой из GitHub
- Домен: `yourapp.onrender.com`

⚠️ **Ограничения:**
- **Сервер засыпает через 15 минут** неактивности
- Первая загрузка после сна ~30-50 сек
- 750 часов работы в месяц (достаточно)
- PostgreSQL база: 90 дней хранения, потом удалится (делай бэкапы!)

❌ **Что нельзя:**
- Свой домен (нужен платный план $7/мес)
- Сервер без засыпания (платный план)
- Больше ресурсов

---

## 🌐 ДРУГИЕ АЛЬТЕРНАТИВЫ (если Render не понравится)

### 1. **Railway.app** ⭐ Рекомендую
- **$5 кредитов в месяц бесплатно**
- Не требует карту
- Не засыпает
- PostgreSQL база включена
- Простой деплой
- **Минус**: $5 хватит на ~20 дней активного использования

**Как деплоить:** Очень похоже на Render, тоже через GitHub

### 2. **Fly.io**
- Бесплатно: 3 micro VMs
- Не засыпает
- **Минус**: требует карту (но не снимают деньги на бесплатном плане)
- Сложнее настраивать

### 3. **Koyeb**
- Бесплатный план: 1 web service
- Не засыпает
- **Минус**: меньше ресурсов, менее стабильный

### 4. **PythonAnywhere** (если передумаешь)
- Есть бесплатный план (если не просит $5)
- Не засыпает
- **Минус**: нужно настраивать вручную, ограничение на CPU

### 5. **Glitch.com**
- Бесплатно
- Простой деплой
- **Минус**: сильные ограничения на ресурсы, засыпает быстро

---

## 📞 НУЖНА ПОМОЩЬ?

1. Проверь логи на вкладке Web
2. Поищи ошибку в Google
3. Напиши мне - покажу логи, помогу разобраться!

---

## ✅ ЧЕКЛИСТ ПЕРЕД ПУБЛИКАЦИЕЙ

- [ ] Установлен Git на компьютере
- [ ] Создан GitHub аккаунт
- [ ] Файл `build.sh` создан
- [ ] Файл `requirements.txt` обновлен (добавлены gunicorn, whitenoise, psycopg2-binary, dj-database-url)
- [ ] `settings.py` обновлен (WhiteNoise, PostgreSQL, ALLOWED_HOSTS)
- [ ] Код загружен на GitHub
- [ ] Создан аккаунт на Render.com
- [ ] Web Service создан и настроен
- [ ] PostgreSQL база создана
- [ ] DATABASE_URL подключен к сервису
- [ ] Сервис успешно задеплоен (проверь Logs)
- [ ] Супер-пользователь создан
- [ ] Сайт открывается без ошибок
- [ ] Админка работает (`/admin/`)
- [ ] Можно зарегистрироваться и войти
- [ ] Можно добавить объявление

---

**🎉 ГОТОВО! Теперь твой сайт доступен всему миру!**

**Твой домен**: `https://golub-bozor.onrender.com` (или как назвал)

---

## 🔥 БОНУС: КАК ИЗБЕЖАТЬ ЗАСЫПАНИЯ (необязательно)

Если хочешь, чтобы сайт не засыпал, используй **UptimeRobot**:

1. Регистрация: https://uptimerobot.com (бесплатно)
2. Создай **New Monitor**:
   - Monitor Type: **HTTP(s)**
   - Friendly Name: `Golub Bozor`
   - URL: `https://golub-bozor.onrender.com`
   - Monitoring Interval: **5 minutes**
3. Нажми **Create Monitor**

UptimeRobot будет пинговать сайт каждые 5 минут, и он не будет засыпать! ⚡

---

## 📞 НУЖНА ПОМОЩЬ?

1. Проверь логи во вкладке **Logs**
2. Поищи ошибку в Google с ключевыми словами из логов
3. Напиши мне - покажи логи, помогу разобраться!

---
